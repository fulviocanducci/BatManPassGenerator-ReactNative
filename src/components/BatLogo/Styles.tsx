import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {},
  title: {
    textAlign: "center",
    fontWeight: "bold",
    fontSize: 29,
    color: "#e5bf3c",
  },
  image: {
    resizeMode: "contain",
    height: 180,
  },
});
